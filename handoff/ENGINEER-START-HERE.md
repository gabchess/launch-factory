# Engineer handoff

Start with [README](../README.md) and [the v2 audit](../docs/V2-AUDIT.md). The active implementation is `launch_factory.py`; the CLI wrapper is `run.sh`. Tests live in `tests/`.

Use the bundled v2 release to render and inspect a package. Keep new output in a new folder. The next major release is v3; proposed work belongs in [next steps](../docs/NEXT-STEPS.md).
