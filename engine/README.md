# Specialist engine

`specialists/` holds the channel protocols, schemas, media recipes, and validators. `scripts/specialist_route.py` selects a protocol and checks its recommendation; it does not generate assets or approve them.

The v2 build is [launch_factory.py](../launch_factory.py). The copy-only v1 scripts, placeholder records, and schemas were removed. Git history retains them.
